var n = 0;
var bg;
var tree;
var leafs = [];
var minHue;
var maxHue;

function setup() {
  createCanvas(900, 700);
  colorMode(HSB);
  noLoop();
  noStroke();
	//need to add createBackground
	tree = createGraphics(width, height);
}


function draw() {
	image(bg, 0, 0);
	//need to add createTree
	//need to add set Hue
	//need to add draw leafs
	image(tree, 0, 0);
	//need to add drawleafs
	//need to add round
	
}
