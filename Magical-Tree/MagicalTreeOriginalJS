var n = 0;
var bg;
var tree;
var leafs = [];
var minHue;
var maxHue;

function setup(){
	size(900, 700);
	colorMode(HSB);
	noLoop();
	noStroke();
	
	createBackground();
	tree = createGraphics(width, height);
	leafs = 
}
